import { SortByAddedToBasketPipe } from './sort-by-added-to-basket.pipe';

describe('SortByAddedToBasketPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByAddedToBasketPipe();
    expect(pipe).toBeTruthy();
  });
});
